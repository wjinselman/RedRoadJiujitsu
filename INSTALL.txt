EARLY CLASS CHECK-IN UPDATE — NOT LIVE UNTIL PUBLISHED

1. Upload the five files inside website/ to the GitHub repository root.
2. Firebase Console > Firestore Database > Rules:
   Replace the previous gymNow, padded, attendanceWindow and planInClass
   functions with attendance-functions.txt. If missing, add them inside the
   database documents block. Do not leave duplicate function definitions.
3. Inside match /attendance/{attendanceId}, replace ONLY its allow create
   condition with attendance-create-condition.txt. Keep all other permissions.
4. If not already done, validAttendance must allow the developer-test source:
   && (data.source == 'kiosk' || data.source == 'member' || data.source == 'developer-test');
5. Publish Firebase Rules. Uploading to GitHub does not publish rules.

Windows are Monday-Friday, Central Time: Kids 5:45 PM to before 7 PM;
Adults 6:45 PM to before 8 PM. Adults cannot select or submit Kids check-ins.
Family accounts and Developer tests must explicitly choose a class.
Family selection records attendance for the account; it does not create
separate named attendance entries for each household member.
Email verification remains a separate access requirement with a clear message.
No new membership, waiver, payment, or role permissions are introduced.
