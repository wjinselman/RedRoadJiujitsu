# Release 53 — current class days

The current schedule is Monday–Friday, kids 6:00–7:00 PM and adults 7:00–8:00 PM. No-Gi remains Tuesday and Thursday nights, as previously listed.

Added the weekdays to the homepage quick facts, schedule introduction, both class cards, location copy and local-area pages. Class times are unchanged. The original schedule flyer is unchanged; the current schedule is written in accessible page text alongside it.

This complete package includes the R51 mobile scroll fix, R52 deletion fix and R49 saved-waiver features. Keep using the existing WebP photos: the reported missing images were confirmed by the user to be a cache issue, so the optional R50 photo workaround is not included in active page references.

Upload the complete website contents to the existing GitHub Pages source, replacing matching files. R49 Firestore rules are included and still need to be published in Firebase if not already done. See MOBILE-RELEASE-49.md for that setup. Historical fix notes describe earlier patches; this full release supersedes those packages.

No live deployment was performed. Isolated checks do not replace the remaining phone and live Firebase checks described in the earlier notes.
