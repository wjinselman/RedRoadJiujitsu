FREE SIGNUP FIX — keep Firebase on Spark. No billing upgrade or Firestore rule changes.
Upload all three files in this ZIP to the website root: members.html, portal.js, email-setup.js.
New signup: separate activation popup asks email, password and confirmation; creates Firebase login; sends verification email; after verification continues existing enrollment. Existing roster records are looked up rather than replaced. Check-in and enrollment already route to members.html?setup=1 and use this same popup.
Existing passwords, member records, payments, attendance and permissions are unchanged. Previously sent email sign-in links remain supported by the existing activate.html path.
Deploy, then reload members.html. The popup must show Choose a password and Create Account. Clint can use his existing roster email. If a login already exists, use Sign In / Forgot password instead.
Tested with mocked Firebase success/failure cases, not by sending real emails or creating production users. Verify one real signup after deployment.
